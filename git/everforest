[delta "everforest"]
    commit-decoration-style = bold box ul
    dark = true
    file-decoration-style = none
    file-style = omit
    hunk-header-decoration-style = "{{bg_blue}}" box ul
    hunk-header-file-style = "{{grey2}}"
    hunk-header-line-number-style = bold "{{bg_green}}"
    hunk-header-style = file line-number syntax
    line-numbers = true
    line-numbers-left-style = "{{bg_blue}}"
    line-numbers-minus-style = "{{red}}"
    line-numbers-plus-style = "{{bg_green}}"
    line-numbers-right-style = "{{bg_blue}}"
    line-numbers-zero-style = "{{grey2}}"
    minus-emph-style = normal "{{red}}"
    minus-style = normal "{{bg_red}}"
    plus-emph-style = syntax "{{green}}"
    plus-style = syntax "{{bg_green}}"
    syntax-theme = Everforest
